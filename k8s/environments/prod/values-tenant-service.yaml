image:
  # TODO: replace with the real ECR URL once real-AWS Terraform is applied
  # (see terraform outputs.ecr_repository_urls).
  repository: docker.io/dip75016860/ai-notification
  tag: "tenant-service-0eabe1bb"

healthGrpcPort: 50053
appGrpcPort: 50153
appGrpcPortEnvName: TENANT_GRPC_PORT

database:
  schemaHash: "d77d6ee898e2"
  enabled: true
  name: tenant_db

env:
  # Stripe billing redirect links (checkout success/cancel, billing portal
  # return) were falling back to the code default (localhost:3000).
  FRONTEND_URL: "https://ainotification.duckdns.org"
