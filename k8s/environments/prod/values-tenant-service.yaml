image:
  # TODO: replace with the real ECR URL once real-AWS Terraform is applied
  # (see terraform outputs.ecr_repository_urls).
  repository: REPLACE_WITH_REAL_ECR_ACCOUNT.dkr.ecr.REPLACE_WITH_REGION.amazonaws.com/ai-notification/tenant-service
  tag: unset

healthGrpcPort: 50053
appGrpcPort: 50153
appGrpcPortEnvName: TENANT_GRPC_PORT

database:
  schemaHash: "initial"
  enabled: true
  name: tenant_db
