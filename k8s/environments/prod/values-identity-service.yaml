image:
  # TODO: replace with the real ECR URL once real-AWS Terraform is applied
  # (see terraform outputs.ecr_repository_urls).
  repository: docker.io/dip75016860/ai-notification
  tag: "identity-service-3f9664b0"

healthGrpcPort: 50052
appGrpcPort: 50152
appGrpcPortEnvName: AUTH_GRPC_PORT

database:
  schemaHash: "df8a743817a3"
  enabled: true
  name: identity_db
