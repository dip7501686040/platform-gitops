image:
  # TODO: replace with the real ECR URL once real-AWS Terraform is applied
  # (see terraform outputs.ecr_repository_urls).
  repository: REPLACE_WITH_REAL_ECR_ACCOUNT.dkr.ecr.REPLACE_WITH_REGION.amazonaws.com/ai-notification/template-service
  tag: unset

healthGrpcPort: 50059
appGrpcPort: 50159
appGrpcPortEnvName: TEMPLATE_GRPC_PORT

database:
  enabled: true
  name: template_db
