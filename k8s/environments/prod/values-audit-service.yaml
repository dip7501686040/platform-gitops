image:
  # TODO: replace with the real ECR URL once real-AWS Terraform is applied
  # (see terraform outputs.ecr_repository_urls).
  repository: REPLACE_WITH_REAL_ECR_ACCOUNT.dkr.ecr.REPLACE_WITH_REGION.amazonaws.com/ai-notification/audit-service
  tag: unset

healthGrpcPort: 50061
appGrpcPort: 50161
appGrpcPortEnvName: AUDIT_GRPC_PORT

database:
  schemaHash: "initial"
  enabled: true
  name: audit_db
